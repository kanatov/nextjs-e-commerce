// In the real roject these will be domain names
// and will be different for each environment.
// For now they're just ports
export const env = {
  api: `http://localhost:${process.env.SERVER_PORT}`,
};
