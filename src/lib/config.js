// Normaly we will have .env file to store these values
// But for this task I will use this config.js file instead

export const env = {
  itemsPerPage: 12,
  "data-api": "http://localhost:3010",
};
